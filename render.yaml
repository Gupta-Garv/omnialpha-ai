services:
  - type: web
    name: omnialpha-ai-agent
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PORT
        value: 8080
      - key: ALPACA_PAPER
        value: "true"
